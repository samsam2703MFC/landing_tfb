{
  "name": "landing-tfb-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ingestion des dépôts GitHub, génération du contenu par l'API Anthropic, écriture dans Directus.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bootstrap": "node bootstrap-directus.mjs",
    "ingest": "node ingest.mjs",
    "ingest:all": "node ingest-all.mjs",
    "check": "node --check ingest.mjs && node --check ingest-all.mjs && node --check bootstrap-directus.mjs && node --check lib/repo.mjs && node --check lib/ai.mjs && node --check lib/directus.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
